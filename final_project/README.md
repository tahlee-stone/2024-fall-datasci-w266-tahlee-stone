# README: Fine-Tuning Cross-Encoders for Detecting Hallucinations in Financial Customer Service Chatbots

This project fine-tunes a **cross-encoder classifier** to detect hallucinations in responses generated by large language models (LLMs). Specifically, it evaluates **groundedness**—the degree to which a model's generated response is faithful to a given context. Hallucination detection is a classification task based on **(query, context, response) triplets. 

This repository contains code and documentation for fine-tuning DeBERTa-v3 cross-encoder for the task of hallucination detection on a HaluBench hallucination benchmark dataset and a synthetically generated RetailBankQA dataset. 

Code is also provided to compare this model to two baselines: 1) a prompted GPT4 LLM evaluator; and 2) an off-the-shelf contextual grounding guardrail from AWS Bedrock. 

Set up for both of these models is detailed below. 

> 📄 For full detail on the project, refer to the main documentation: `DATASCI W266 - Final Project Model Documentation - Tahlee Stone.pdf`

---

## 📁 Project Structure

```
.
├── data/                         # Raw and processed datasets
├── documentation/                # Latex build of final report
├── results/                      # Model outputs and evaluation metrics
├── scripts/                      # Code for model training and evalaution
├── README.md                     # Project instructions and documentation
├── requirements.txt              # Package dependencies
```

---

## 📚 Dataset Sources

### HaluBench Benchmark

- \~15,000 (query, context, response) triplets annotated for hallucination detection
- Subsets used: DROP, FinanceBench, PubMedQA, RAGTruth
- Excludes HaluEval and COVID-QA due to length and label bias
- See for further detail:
  https://huggingface.co/datasets/PatronusAI/HaluBench
  https://arxiv.org/abs/2407.08488


### RetailBankQA Synthetic Dataset

- 2,770 synthetic examples based on Banking77 intents (https://huggingface.co/datasets/PolyAI/banking77)
- Contexts generated using `gpt-4o-mini`, grounded responses via `gpt-4o`, hallucinated responses via `gpt-3.5-turbo`
- Synt
- Labels verified using OpenAI `o3-pro` model with 90% agreement.
- Note: Synthetic FAQ-style contexts were used instead of scraping real banking help pages due to commercial and ethical considerations. While early prototypes used BeautifulSoup to scrape public retail bank FAQs, the final dataset was generated using LLMs to avoid intellectual property issues and ensure reproducibility across institutions.


### Banking77 Dataset (Original Input Queries)

- Source: [PolyAI Banking77](https://huggingface.co/datasets/PolyAI/banking77)
- Downloaded from: https://github.com/PolyAI-LDN/task-specific-datasets/tree/master/banking_data
- Dataset used: `banking_77_train_set.csv`
- This dataset provides 10,003 customer support queries across 77 banking-specific intent classes.
- Used as the initial source of real-world customer queries for generating the synthetic RetailBankQA dataset.

- Downloaded from: https://github.com/PolyAI-LDN/task-specific-datasets/tree/master/banking_data
- Dataset used: `banking_77_train_set.csv`
- This dataset provides 10,003 customer support queries across 77 banking-specific intent classes.
- Used as the initial source of real-world customer queries for generating the synthetic RetailBankQA dataset.

---

## ⚙️ Environment Setup

### Prerequisites

- Python >= 3.10
- `pip` or `poetry`
- [HuggingFace Transformers](https://huggingface.co/transformers), `datasets`, `pandas`, `scikit-learn`

### Virtual Environment (Recommended)

```bash
python -m venv venv
source venv/bin/activate  # or .\venv\Scripts\activate (Windows)
```

### Package Installation

```bash
pip install -r requirements.txt
```

Or manually:

```bash
pip install transformers datasets scikit-learn pandas seaborn openai jupyter
```

---

## ☁️ AWS Bedrock Setup

### 1. **AWS Account Creation & Permissions**

- Sign up at: [https://aws.amazon.com/](https://aws.amazon.com/)
- Navigate to IAM Console
  - Create an IAM user with programmatic access
  - Attach the following policies:
    - `AmazonBedrockFullAccess`
    - `AmazonAPIGatewayInvokeFullAccess`

### 2. **Access Key Configuration**

Create a `.env` or use shell variables:

```bash
export AWS_ACCESS_KEY_ID="your-access-key"
export AWS_SECRET_ACCESS_KEY="your-secret-key"
export AWS_REGION="us-east-1"
```

### 3. **Enable Guardrails**

- Navigate to Bedrock console > Guardrails
- Create a contextual grounding guardrail
  - Enable *Contextual Grounding*
  - Disable *Relevance checks*
  - Set *Threshold* to `0.5`
- Use the `apply_guardrail()` API endpoint in scripts with Claude Sonnet 4 as the model

---

### 🔐 OpenAI API Key Setup

To use the GPT-based evaluators (e.g., GPT-4o, GPT-3.5-Turbo), you must provide an OpenAI API key.

**Create/OpenAI Account**:  
Sign up at [https://platform.openai.com](https://platform.openai.com)

**Generate an API Key**:  
Navigate to: **Account → API Keys → Create new key**

**Configure Environment**:  
You can either:

- **Set via shell**:

```bash
export OPENAI_API_KEY="your-openai-key"
```
- **Or create a .env file**:

Insert OPENAI_API_KEY=your-openai-key

---
### 🧪 Google Colab Pro Setup (Optional)

To accelerate training:
1. Subscribe to [Google Colab Pro](https://colab.research.google.com/signup)
2. Select:
   - Runtime > Change Runtime Type
   - Hardware accelerator: **GPU (T4)**
   - Runtime shape: **High RAM**

> Note: You still need to batch inputs and apply chunking for long contexts to avoid out-of-memory issues.

---

## 🚀 Running the Project

### Data Preparation

- `scripts/run_synthetic_data_construction.ipynb`

### Training & Inference

- `scripts/run_main_model.ipynb` – Fine-tune DeBERTa-v3
- `scripts/run_hyperparameter_search.ipynb` – Tune learning rate, batch size

### Baseline Comparisons

- `scripts/run_baseline_comparison_models.ipynb` – GPT-4o & AWS Bedrock
- `scripts/run_model_evaluation.ipynb` – Evaluate using macro metrics

### Error Analysis

- `scripts/run_error_analysis.ipynb`

---

## 🧠 Key Technical Details

- **Model**: DeBERTa-v3-base cross-encoder with added classification head
- **Input Format**: `[CLS] query [SEP] context [SEP] response [SEP]`
- **Loss**: Binary cross-entropy
- **Chunking Strategy**: Overlapping windowing for inputs >512 tokens
- **Metrics**: Accuracy, Macro Precision/Recall/F1
- **Baselines**:
  - GPT-4o zero-shot with structured JSON prompt (Jinja2)
  - AWS Bedrock contextual grounding API

---

## 📌 Notes

- The model is designed for deterministic outputs and reproducible audits
- GPT-based baselines offer richer reasoning but suffer latency and cost trade-offs.
- AWS Bedrock guardrails are useful but opaque and inflexible.

---

## 📬 Contact

Tahlee Stone\
University of California, Berkeley\
[*tahlee.stone@berkeley.edu*](mailto\:tahlee.stone@berkeley.edu)

